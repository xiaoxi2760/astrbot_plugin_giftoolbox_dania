# 更新日志

## 1.2.0

- `多图合成gif` 改名 **`表情合成`**，用法不变
- 修复 `表情合成` 只能收到一张图的问题：现在回复含图的消息、合并转发也能正常取到图
- 修复 `表情合成` 结果被 QQ 显示为静态图：改成单独一条纯图片消息发送
- 只收到一张图时会提示「一帧是动不起来的」，不再默默返回静图
- **移除** `合成1gif` / `合成2gif`（精灵图合成），对应的 `yy合成1` / `yy合成2` 别名一并移除
- **移除** `danya_name` 配置项，昵称固定为「娅娅」，不再支持自定义

## 1.1.0

- `多图合成gif` 支持**动图按顺序拼接**：把多个动图按发出的顺序接成一条，各段沿用原本的播放速度
- 新增 `result_split_mb` 配置项：结果动图超过该体积时拆分发送，避免 QQ 把动图转成静态图
- 移除 `变快` / `变慢` 两个未公开的别名，统一用 `加速` / `减速`

## 1.0.0

- 达妮娅（娅娅）风格化文案，支持 `yy` / `娅娅` / `danya` 前缀别名，原有指令全部保留
- 高倍速加速、倒放、做旧等「反转/失真」场景会触发黑化形态彩蛋
